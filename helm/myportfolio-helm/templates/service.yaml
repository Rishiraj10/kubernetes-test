apiVersion: v1
kind: Service
metadata:
  name: {{ include "myportfolio-helm.fullname" . }}
  labels:
    {{- include "myportfolio-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "myportfolio-helm.selectorLabels" . | nindent 4 }}
