apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "myportfolio-helm.fullname" . }}-test-connection"
  labels:
    {{- include "myportfolio-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "myportfolio-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
